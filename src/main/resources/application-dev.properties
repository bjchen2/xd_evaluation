#开发环境配置

#数据库配置
spring.datasource.url=jdbc:mysql://120.78.131.71:3333/xd_evaluation?characterEncoding=utf8&useSSL=false
spring.datasource.driver-class-name=com.mysql.jdbc.Driver
spring.datasource.username=xd_evaluation
spring.datasource.password=xd_evaluation

#数据库连接池配置（需先在pom中引入druid数据库连接池）
#初始化连接池大小，最大、最小连接数
spring.datasource.druid.initial-size=5
spring.datasource.druid.min-idle=5
spring.datasource.druid.max-active=20
# 配置获取连接等待超时的时间
spring.datasource.druid.max-wait=60000
# 配置间隔多久才进行一次检测，检测需要关闭的空闲连接，单位是毫秒
spring.datasource.druid.time-between-eviction-runs-millis=60000
# 配置一个连接在池中最小生存的时间，单位是毫秒
spring.datasource.druid.min-evictable-idle-time-millis=300000

#显示JPA sql语句
#spring.jpa.properties.hibernate.show_sql=true
#spring.jpa.properties.hibernate.format_sql=true

#mybatis有关配置
#显示mybatis sql语句,需将控制台日志级别改为debug级别
logging.level.com.wxsell.domain.mapper = trace
#配置mapper扫描路径
mybatis.mapper-locations=classpath:mapper/*.xml

#微信小程序有关配置
wechat.appid =1344
wechat.secret =4444
wechat.msgDataFormat=JSON

#配置redis
spring.redis.host = 最后再测试
spring.redis.port =
#没有密码可以不写该属性
spring.redis.password =
# 连接池最大连接数
spring.redis.jedis.pool.max-active=5000
# 连接池最大阻塞时间
#spring.redis.jedis.pool.max-wait=5000
# 连接池最大空闲连接数
spring.redis.jedis.pool.max-idle=30000
# 连接池最小空闲连接数
spring.redis.jedis.pool.min-idle=50
# 连接超时时间
spring.redis.timeout=20s